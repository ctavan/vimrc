#!/bin/bash -eu

ln -sf `pwd`/bin/in-new-term-function /usr/local/bin/in-new-term-function

brew update
brew reinstall fzf
brew upgrade --HEAD fzf
/usr/local/opt/fzf/install

git submodule update --init --recursive
